[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn -w 2 --bind 0.0.0.0:$PORT wsgi:app"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
